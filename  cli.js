const colors = require("colors");

console.log("Hello world".rainbow);
console.log("yezze.com".bgBlue);
console.log("google.com".bgYellow);

//En el package.json hay un objeto que se llama scripts, este me permite crear comandos y en vez de colocar node nameFile coloco lo que escribo ahi
// npm install nodemon -D lo instala en las dependencias de desarrollo
//npm install nodemon -g lo instala en el computador
//npx permite ejecutar algunas aplicaciones de consola que pueden instalarse con node
//CLI (Common line interface) herramientas que funcionan desde consola
//En la carpeta .bin estan los programas ejecutables de consola
//npx es un ejecutador de paquetes, ejecuta algo sin la necesidad de instalarlo
